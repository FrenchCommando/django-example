```shell
apt-get update && apt-get upgrade
```

- set hostname

```shell
hostnamectl set-hostname toto
hostname
```

- host names

```shell
nano /etc/hosts
```

```shell
ipaddress	servername
```

- user

```shell
adduser toto
```

```shell
adduser toto sudo
```

